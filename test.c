#include <stdio.h>

int main()
{

   // char *a = "hello"; //Declaración de variable entera de tipo entero
   // char **puntero; //Declaración de variable puntero de tipo entero
   // puntero = &a; //Asignación de la dirección memoria de a

   // char * palabras[] = {"hola","hghla","hsdla","hsd"} ;

	// printf("p[2] = %s\n", palabras[2]);

   // printf("El valor de a es: %s. \nEl valor de *puntero es: %s. \n",a, puntero[0]);
   // printf("La dirección de memoria de *puntero es: %p",puntero);
   int i = 0;
   for (i = 0; i < 10; i++);
   printf("esto es i %d", i);
   return 0;
}